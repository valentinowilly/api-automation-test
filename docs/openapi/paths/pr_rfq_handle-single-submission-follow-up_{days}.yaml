post:
  summary: Handle single submission / one pending follow-up
  deprecated: false
  description: |
    **Cron Job / Manual Trigger**

    Handle RFQs with one vendor submitted and one pending at N days.

    **Logic**: Sends CS notification about pending vendor status.

    **Days Parameter**: Optional, defaults to configured value.
  operationId: handleSingleSubmissionFollowUp
  tags:
    - Cron Jobs & Manual Triggers
  parameters:
    - name: days
      in: path
      description: ''
      required: false
      example: 0
      schema:
        type: integer
  responses:
    '200':
      description: Follow-up processed successfully
      content:
        application/json:
          schema:
            type: object
            properties: {}
      headers: {}
  security:
    - bearer: []

post:
  summary: Handle declined/pending follow-up
  deprecated: false
  description: |
    **Cron Job / Manual Trigger**

    Handle RFQs with mixed declined and pending items.

    **Logic**: Sends separate notifications for each status type to CS.

    **Execution**: Can be triggered manually or via cron schedule.
  operationId: handleDeclinedPendingFollowUp
  tags:
    - Cron Jobs & Manual Triggers
  parameters: []
  responses:
    '200':
      description: Follow-up processed successfully
      content:
        application/json:
          schema:
            type: object
            properties: {}
      headers: {}
  security:
    - bearer: []
